<%- include('./partials/header') %>

<div class="w-full min-h-screen flex items-start px-20 py-20 bg-gradient-to-r from-blue-50 to-purple-50">
  <div class="w-[25%] p-8 bg-white shadow-lg rounded-lg">
    <div class="flex flex-col space-y-4">
      <a href="#" class="text-gray-600 hover:text-blue-500 transition-all">All Products</a>
      <a href="#" class="text-gray-600 hover:text-blue-500 transition-all">Create New Product</a>
      <a href="#" class="text-red-500 hover:text-red-600 transition-all">Delete All</a>
    </div>
  </div>

  <div class="w-[75%]">
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
      <% products.forEach(function(product){ %>
        <div class="bg-white rounded-xl shadow-md overflow-hidden hover:shadow-lg transition-all">
          <div class="w-full h-64 bg-yellow-500"></div>
          <div class="p-4">
            <h3 class="text-lg font-bold text-gray-800"><%= product.name %></h3>
            <h4 class="text-gray-600">₹ <%= product.price %></h4>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</div>

<%- include('./partials/footer') %>